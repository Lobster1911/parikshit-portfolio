import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import path from 'path';
import {defineConfig} from 'vite';

export default defineConfig(() => {
  return {
    base: './',
    plugins: [react(), tailwindcss()],
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
    build: {
      rollupOptions: {
        input: {
          main: path.resolve(__dirname, 'index.html'),
          projectlist: path.resolve(__dirname, 'Projects/projectlist.html'),
          project1: path.resolve(__dirname, 'Projects/project1.html'),
          project2: path.resolve(__dirname, 'Projects/project2.html'),
          project3: path.resolve(__dirname, 'Projects/project3.html'),
          project4: path.resolve(__dirname, 'Projects/project4.html'),
          project5: path.resolve(__dirname, 'Projects/project5.html'),
        },
      },
    },
    server: {
      // HMR is disabled in AI Studio via DISABLE_HMR env var.
      // Do not modifyâfile watching is disabled to prevent flickering during agent edits.
      hmr: process.env.DISABLE_HMR !== 'true',
      // Disable file watching when DISABLE_HMR is true to save CPU during agent edits.
      watch: process.env.DISABLE_HMR === 'true' ? null : {},
    },
  };
});
